#include <iostream>
#include <math.h>
using namespace std;
int main()
{
    int a, b, c, s, d;
    cin>>a>>b>>c;
    d=round((a*b)/(c*c));
    s=abs((a*b)-(c*c));
    cout<<d<<endl<<s;
    return 0;
}
